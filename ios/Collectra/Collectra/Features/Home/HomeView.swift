import SwiftUI

/// Placeholder for this phase — Phase 4/5 will build the real dashboard
/// (portfolio value, insights, etc.) here without needing to touch
/// MainTabView's navigation structure.
struct HomeView: View {
    var body: some View {
        PlaceholderScreen(title: "Home", systemImage: "house.fill")
    }
}
